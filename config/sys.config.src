%% Substituted by relx at build; ${VAR} resolves from the environment at boot.
%%
%% The sensor's own tuning (sources, poll interval, seed, dedupe window) is read
%% straight from the environment in code (HECATE_NEWS_FEEDS, HECATE_NEWS_POLL_MS,
%% HECATE_NEWS_SEED_COUNT, HECATE_NEWS_MAX_SEEN), falling back to the app.src
%% defaults — so it does not appear here and an unset var is never a malformed
%% term. This file carries only the mesh wiring, which every ${VAR} must resolve.
[
    {hecate_om, [
        {service_cert_path, "/etc/hecate/secrets/service-cert.pem"},
        {station_socket,    "/run/macula/station.sock"},
        {health_port,       ${HECATE_HEALTH_PORT}},
        {capability_topic,  <<"_mesh.cap.">>},
        %% Same realm as the society whose feed we publish to, so the minds hear
        %% the news sensor.
        {realm,             <<"${HECATE_REALM}">>}
    ]},

    {kernel, [{logger_level, info}]}
].
